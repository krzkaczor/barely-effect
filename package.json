{
  "name": "barely-effect-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "fmt": "oxfmt --check",
    "fmt:fix": "oxfmt",
    "lint": "bun run --workspaces lint",
    "lint:fix": "bun run --workspaces lint:fix",
    "test": "bun run --workspaces test",
    "fix": "bun run lint:fix && bun run fmt:fix && bun test"
  },
  "dependencies": {
    "effect": "4.0.0-beta.25",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.20.0",
    "oxlint": "^1.35.0",
    "oxlint-tsgolint": "^0.10.0"
  }
}
